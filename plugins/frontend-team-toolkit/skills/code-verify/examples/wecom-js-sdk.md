# 案例：企业微信 JS-SDK（H5 内置浏览器）

> **性质**：本文件为 `code-verify` 技能的应用示例之一，**非**技能默认假设或边界限制。  
> 执行技能时以用户实际提供的工具名为准；仅当用户上下文明确为企业微信对接时，可参考本案例。

---

**场景**：企业微信内置浏览器 H5 对接，不熟悉领域，AI 全信导致 7–8 轮迭代、约 3 小时后发现方向可能一开始就错。

**阶段 1**：应先要方案，强制 AI 标注 `wx.config` vs `agentConfig` 先后等不确定点。

**阶段 2 文档锚**：[企业微信 JS-SDK 说明](https://developer.work.weixin.qq.com/document/path/90514)

**阶段 2 踩坑**：AI 将「注册应用身份」与「注入 JS-SDK 配置」混为一体 → **维度 5 初始化顺序 ❌**

**阶段 3 MVU 建议**：

1. 验证 `wx.config` / `agentConfig` 注入（文档步骤一、二）
2. 验证 `getCurExternalContact` 等会话 API
3. 验证打开外部会话等核心能力

**阶段 4**：完全陌生领域应在 **60 分钟**止损；本次耗约 3 小时属反例。

**阶段 5**：将「按文档章节拆 MVU，不按 AI 文件结构拆」写入检查清单。
